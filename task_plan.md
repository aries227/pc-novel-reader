# 简阅 v0.5.0 任务计划

目标：按用户最新需求扩展阅读器——书架改名、多种翻页模式、阅读高亮、大词典与用户词典导入、练习自定义（题量/难度/提示词）、右侧分栏面板（边翻边做）。

## 阶段

1. [x] 书架改名：LibraryStore.rename + IPC + 书架 UI 改名按钮
2. [x] 翻页模式：paged(左右) / vertical(上下) / scroll(滚动) + 阅读器模式切换
3. [x] 高亮：Highlight 存储 + 渲染包裹 + 选词弹窗高亮/取消
4. [x] 词典扩容 + 考试标签：内置 77,854 词条 + 14,275 带标签
5. [x] 用户词典导入：CSV/TXT/JSON 解析 + 合并查询 + 导入入口
6. [x] 练习自定义：题量/难度/自定义提示词（设置项 + 面板控件 + 出题逻辑）
7. [x] 右侧分栏面板：翻译/词典/练习 Tab，练习可收起继续翻页
8. [ ] 全量测试、构建、v0.5.0 发布

## 决策

- 内置词典扩容：以 ECDICT dict.json 为准，保留 translation 非空且带频率/标签的词条，控制 JSON 在可接受体积内。
- 用户词典格式：JSON（词条数组/对象）、ECDICT CSV、TXT（word<TAB>translation）。
- 练习自定义提示词：设置中提供 textarea，非空时替换默认系统提示词（保留 JSON 输出约束）。
- 高亮按书/章/段落文本存储，渲染时包裹 `<mark>`；避免把高亮逻辑写进 library.json 之外的文件。

## 已完成

- v0.4.1：修复 electron-updater ESM 具名导入导致的打包版启动崩溃（已发布）。
